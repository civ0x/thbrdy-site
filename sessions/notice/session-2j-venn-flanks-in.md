# Session 2j: Venn — Move Flank Labels Closer to Circles

In `src/components/islands/NoticeCompetitiveGap.tsx`, two changes:

### 1. Left overlap label — move right

Change `left` from `"-2%"` to `"4%"`.

### 2. Right overlap label — move left

Change `right` from `"-2%"` to `"4%"`.

Do not change anything else.
